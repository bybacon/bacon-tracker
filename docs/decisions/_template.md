---
status: proposed
date: 2026-01-01
---

# Name the decision

- Date: 2026-01-01
- Related: -

## Status

Proposed

## Context

What is the issue that we're seeing that is motivating this decision or change?
What options were weighed, and what is already true that constrains them?

## Decision

What is the change that we're proposing and/or doing? Be specific enough that
someone can tell whether an implementation follows it.

## Consequences

What becomes easier or more difficult because of this change? Record the costs
as plainly as the benefits - a Consequences section with no costs in it means
the decision was not examined.

<!--
Filename: <NS>-ADR-NNNN-slug.md, in the directory named for its status.
Take NNNN from .next-id. `status` must match that directory; `date` is the day
the decision took effect and is ISO-8601 - replace the placeholder above.

`status` and `date` are the only required keys. Add these only when they have a
value, and delete the ones that do not:

  deciders: [name]
  supersedes: [<NS>-ADR-NNNN]
  superseded_by: [<NS>-ADR-NNNN]
  stories: [<NS>-NNN]
  canonical: <NS>-ADR-NNNN     # only when adopting another project's decision
  tags: [a-tag]

An accepted record is immutable - change it by appending
"## Amendment - YYYY-MM-DD (STORY)", or supersede it with a new one.
-->
